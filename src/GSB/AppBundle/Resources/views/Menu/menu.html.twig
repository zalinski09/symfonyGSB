{#Menu de l'application#}
<nav class="navbar navbar-default navbar-fixed-top topnav" role="navigation">
    <nav class="container topnav">
        <nav class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand topnav" href="{{ path('gsb_app_homepage') }}">GSB - Gestion de vos frais </a>
        </nav>

        <nav id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false">Fiche de frais <b class="caret"></b></a>
                    <ul class="dropdown-menu">

                        {% if is_granted('ROLE_USER') %}
                            <li><a href="{{ path('gsb_app_indexFrais') }}">Enregistrer nouvelle</a></li>
                            <li><a href="{{ path('gsb_app_ficheDeFrais') }}">Consultation</a></li>
                        {% endif %}

                        {% if is_granted('ROLE_COMPTABLE') %}
                            <li><a href="{{ path('gsb_app_validerFicheDeFrais') }}">Validation</a></li>
                        {% endif %}

                    </ul>
                </li>
                {% if is_granted('ROLE_COMPTABLE') %}
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false">Paiement <b class="caret"></b></a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="{{ path('gsb_app_suiviFicheDeFrais') }}">Suivis paiement</a></li>
                    </ul>
                </li>
                {% endif %}
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if is_granted('ROLE_USER') %}
                    <li><a href="#">Bienvenue visiteur {{ app.user.login }}  </a></li>
                {% endif %}
                <li>
                    {% if app.user %}
                        <a href="{{ path('logout') }}"> Se déconnecter</a>
                    {% else %}
                        <li><a href="{{ path('login') }}">Connectez vous</a></li>
                    {% endif %}
                </li>
            </ul>


        </nav><!--/.nav-collapse -->
    </nav>
</nav>
